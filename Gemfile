# Only used for previewing the site on your own computer (see README).
# GitHub Pages ignores this file and uses its own copy of the same gem,
# so the site publishes fine even if you never run anything locally.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Standard-library pieces that newer versions of Ruby no longer ship by default.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "webrick"
